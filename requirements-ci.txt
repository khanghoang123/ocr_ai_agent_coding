# Lightweight dependency set for CI quality gates.
# Keep this list focused on linting + non-inference tests.

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.6.0

numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0,<4.13.0
PyYAML>=6.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

fastapi>=0.110.0
python-multipart>=0.0.9

streamlit>=1.32.0
requests>=2.31.0

# PDF export uses reportlab; the white-bg PDF export tests exercise
# the full export pipeline so reportlab needs to be available in CI.
reportlab>=4.0.0
# pymupdf (imported as ``fitz``) is used by the PDF tests to inspect
# the rendered output (page rect, embedded image presence, text layer).
pymupdf>=1.23.0
